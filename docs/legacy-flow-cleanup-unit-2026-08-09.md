# 폐기 흐름·정적 경고 정리 단위

- 기준일: 2026-08-09
- 목표: 기획 5의 비공개 동적 금기어 흐름을 과거 질문·금기어 공개 코드가 다시 침범하지 못하게 한다.

## 제거·격리

- 런타임에서 참조되지 않던 `Onboarding.tsx`, `ForbiddenReveal.tsx`를 삭제했다.
- 클라이언트 게임 단계에서 `onboarding`, `reveal`을 제거했다.
- 질문 답변과 질문 목록을 보관하던 사용하지 않는 스토어 상태·액션을 제거했다.
- 이미 제품 경로에서 비활성이고 영구 skip이던 과거 온보딩→프롭 테스트를 삭제했다.
- 서버는 과거 `onboarding_complete`, `request_onboarding_questions` 메시지를 계속 명시 거부한다. 이는 옛 클라이언트의 금기어 공개 우회를 차단하는 호환성 경계다.

## 코드 품질

- 원본 엘리베이터 리그의 조건부 콜백 표현을 optional chaining으로 바꿔 사용하지 않는 표현 경고를 제거했다.
- 렌더러와 정적 데이터 계약을 의도적으로 함께 내보내는 두 모듈에는 Fast Refresh 규칙 예외 이유를 파일에 명시했다.
- `oxlint` 오류·경고 0개를 확인했다.

## 검증

- 서버 전체 테스트: `385 passed` — skip 0
- 클라이언트 TypeScript·프로덕션 빌드 통과
- 학교 구조 계약 검증 통과
- 삭제한 클라이언트 심볼의 잔여 참조 없음
